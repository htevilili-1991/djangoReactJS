# Copy to .env and adjust for your environment
# Optional: override these for production (Django reads os.environ)

DB_NAME=djangoreactjs
DB_USER=djangoreactjs
DB_PASSWORD=djangoreactjs_secret
DB_HOST=localhost
DB_PORT=5432
